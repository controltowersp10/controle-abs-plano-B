specVersion: "v1beta"
serviceId: "controle-abs"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "controle-abs-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
